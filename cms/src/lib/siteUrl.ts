// The public frontend's base URL — used to build "View Page" preview links
// from the admin panel. Same env var payload.config.ts uses for CORS.
export const SITE_URL = process.env.SITE_URL || 'https://gothamdataclinic.org'
