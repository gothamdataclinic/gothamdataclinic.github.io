{
  "name": "gdc-website",
  "private": true,
  "description": "Gotham Data Clinic — monorepo for the public website (website/) and Payload CMS (cms/)",
  "scripts": {
    "install:all": "npm install --prefix website && npm install --prefix cms",
    "dev:website": "npm run dev --prefix website",
    "dev:cms": "npm run dev --prefix cms",
    "build:website": "npm run build --prefix website",
    "build:cms": "npm run build --prefix cms",
    "start:cms": "npm run start --prefix cms",
    "preview:website": "npm run preview --prefix website"
  }
}
